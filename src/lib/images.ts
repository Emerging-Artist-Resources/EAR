/** Default quality for marketing / service page photos (Next default is 75). */
export const MARKETING_IMAGE_QUALITY = 90 as const

/** Half-width column in a 2-col grid; full width on mobile. */
export const DOCUMENTATION_SERVICE_IMAGE_SIZES =
  "(max-width: 1024px) 100vw, (min-width: 1025px) 50vw" as const

/** Full-bleed bio section background. */
export const DOCUMENTATION_BIO_SECTION_SIZES = "100vw" as const

export const SERVICES_HERO_IMAGE_SIZES = "100vw" as const
