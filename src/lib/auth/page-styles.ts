/** Shared layout classes for standalone auth pages (sign-in, sign-up, password reset). */
export const AUTH_PAGE_SHELL_CLASS =
  "min-h-screen flex items-center justify-center bg-ear-off-white py-12 px-4 text-ear-black sm:px-6 lg:px-8" as const

export const AUTH_PAGE_CARD_CLASS = "border-ear-black/10 bg-white shadow-sm" as const

export const AUTH_LINK_CLASS = "text-ear-dark-red hover:opacity-80" as const

export const AUTH_MUTED_TEXT_CLASS = "text-ear-black/70" as const
