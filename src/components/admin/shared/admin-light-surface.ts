/**
 * Light panel styling for admin UI. The global body uses `color: var(--foreground)` (off-white)
 * for the dark shell; any white/light surface must set an explicit dark text color.
 */
export const ADMIN_LIGHT_SURFACE = "bg-white text-[var(--gray-900)]" as const
